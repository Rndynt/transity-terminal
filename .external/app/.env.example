# === SERVER ===
PORT=5000
NODE_ENV=production

# === CONSOLE URL (WAJIB) ===
# URL TransityConsole — semua API diteruskan ke sini.
# TransityApp tidak pernah langsung ke Terminal.
CONSOLE_URL=https://console.transity.web.id
